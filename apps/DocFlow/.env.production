# Production values are injected by the deployment platform's environment
# configuration — never committed to the repository. This file intentionally
# contains no live endpoints. See /.env.example for the variable list.
